import React from 'react';
import classes from './Main.module.css';

const Main = () => {
    return (
        <section>
            <main className={classes.container}>
            </main>
        </section>

    );
}

export default Main;